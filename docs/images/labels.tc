// `<` `>` がただの文字として扱われることを確認するサンプル
// 信号名・矢印ラベル中の `<` `>` は XML エスケープして SVG に出力される。
// レベル文字列中の `<` / `>` は ParseError::InvalidLevelChar になるので使えない。
@step 10
@slant 2

// --- 信号名中の `<` `>` ---
"<request>"   _~~~__~~~__
"<grant>"     ___~~~__~~~_
ack           __~~__~~__~~

// --- 矢印ラベル中の `<` `>` ---
Clock      _~@{rise}_~@{fall}_~_~
Data       _____~~~~~~~~_______

@-> (@{rise}, @{fall}) <signal-set>
